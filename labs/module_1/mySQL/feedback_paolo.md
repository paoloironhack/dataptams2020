### Good work, it works with workbench but it would not work directly with mysql command line because you do not use the
## statement "create database" and "use database" in create.sql and seeding.sql.